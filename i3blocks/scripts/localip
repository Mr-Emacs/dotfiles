#!/usr/bin/env bash
IP=$(ip -4 addr show scope global | grep -oP '(?<=inet\s)\d+(\.\d+){3}' | head -n 1)

if [ -n "$IP" ]; then
    echo "Leaked IP: $IP"
    echo "Leaked IP"
    echo "#00FF00"   # green
else
    echo "Leaked IP: none"
    echo "Leaked IP"
    echo "#FF0000"   # red
fi

