# Tokyo Night Colors
tokyo_night_bg="#1a1b26"
tokyo_night_fg="#c0caf5"
tokyo_night_cyan="#7dcfff"
tokyo_night_green="#9ece6a"
tokyo_night_yellow="#e0af68"
tokyo_night_red="#f7768e"
tokyo_night_magenta="#bb9af7"
tokyo_night_blue="#7aa2f7"
tokyo_night_black="#414868"
tokyo_night_brightblack="#565f89"
tokyo_night_white="#c0caf5"

# Base settings
set-option -g base-index 1
set-option -g pane-base-index 1
set-window-option -g mode-keys vi
set-option -g set-clipboard on
set -g mouse on
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm*:Tc"
set -g focus-events on
set-option -g default-shell /usr/bin/fish

# Key bindings
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "xclip -selection clipboard"
<<<<<<< HEAD
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# Tokyo Night Theme
set -g status-style "bg=$tokyo_night_bg,fg=$tokyo_night_fg"
set -g status-left-style "bg=$tokyo_night_blue,fg=$tokyo_night_bg"
set -g status-right-style "bg=$tokyo_night_bg,fg=$tokyo_night_fg"

set -g status-left "#[bg=$tokyo_night_blue,fg=$tokyo_night_bg,bold] #S #[bg=$tokyo_night_bg,fg=$tokyo_night_blue]"
set -g status-right "#[fg=$tokyo_night_brightblack]#[bg=$tokyo_night_brightblack,fg=$tokyo_night_fg] %Y-%m-%d #[fg=$tokyo_night_blue]#[bg=$tokyo_night_blue,fg=$tokyo_night_bg,bold] %H:%M "

set -g status-left-length 100
set -g status-position bottom
set -g status-justify left

# Window status
set -g window-status-current-format "#[fg=$tokyo_night_bg,bg=$tokyo_night_magenta]#[fg=$tokyo_night_bg,bg=$tokyo_night_magenta,bold] #I #W #[fg=$tokyo_night_magenta,bg=$tokyo_night_bg]"
set -g window-status-format "#[fg=$tokyo_night_fg,bg=$tokyo_night_black] #I #W #[fg=$tokyo_night_black,bg=$tokyo_night_bg]"

# Pane borders
set -g pane-border-style "fg=$tokyo_night_brightblack"
set -g pane-active-border-style "fg=$tokyo_night_blue"

# Message style
set -g message-style "bg=$tokyo_night_blue,fg=$tokyo_night_bg"
set -g message-command-style "bg=$tokyo_night_blue,fg=$tokyo_night_bg"

# Copy mode
set -g mode-style "bg=$tokyo_night_blue,fg=$tokyo_night_bg"

bind-key f run-shell "tmux neww bash ~/scripts/tmux_sessionizer"
=======
set -g focus-events on
>>>>>>> origin/master
